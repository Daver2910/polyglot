import React from 'react';

const LanguageSelector = () => {
  return (
      <ui className={'dropdown'}>
          <i className={'flag us'}></i>
          <i className={'flag fr'}></i>
          <i className={'flag de'}></i>
          <i className={'flag ca'}></i>
      </ui>

  )  ;
};

export default LanguageSelector;
